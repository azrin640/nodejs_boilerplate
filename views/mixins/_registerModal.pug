mixin registerModal( user = {} )

  div(class="modal modal-register")
    .modal-background
    .modal-header
    .modal-content
      .card
        .card-header
          .card-header-title
            h3 Register
        .card-content
          div(class="modal-message")
          form(class="modal-register-form" action="/register" method="POST")
            div(class="field")
              label(for="name" class="label") Name
              p(class="control has-icons-left has-icons-right")
                input(class="input" type="text" name="name" id="name" placeholder="Name" required)
                span(class="icon is-small is-left")
                  i(class="fas fa-user")
                span(class="icon is-small is-right")
                  i(class="fas fa-check")
            div(class="field")
              label(for="email" class="label") Email
              p(class="control has-icons-left has-icons-right")
                input(class="input" type="email" name="email" id="email" placeholder="Email" required)
                span(class="icon is-small is-left")
                  i(class="fas fa-envelope")
                span(class="icon is-small is-right")
                  i(class="fas fa-check")
            div(class="field")
              label(for="password" class="label") Password
              p(class="control has-icons-left has-icons-right")
                input(class="input" type="password" name="password" id="password" placeholder="Password" required)
                span(class="icon is-small is-left")
                  i(class="fas fa-lock")
                span(class="icon is-small is-right")
                  i(class="fas fa-check")
            div(class="field")
              label(for="password" class="label") Repeat Password
              p(class="control has-icons-left has-icons-right")
                input(class="input" type="password" name="password_repeat" id="password_repeat" placeholder="Repeat Password" required)
                span(class="icon is-small is-left")
                  i(class="fas fa-lock")
                span(class="icon is-small is-right")
                  i(class="fas fa-check")
          div(class="field is-grouped")
            div(class="buttons is-right")
              a(class="button is-primary modal-register-button" type="submit") 
                i(class="fas fa-sign-in-alt") 
                span &nbsp;Register                  
              a(class="button is-danger modal-close-register") 
                i(class="fas fa-times")
                span &nbsp;Cancel
          div(class="message")
        .card-footer.is-right
          p Already Registered ?
          a(class="login-modal-button" href="#") &nbsp;Login
    .modal-footer            
    button(class="modal-close modal-close-register is-large" aria-label="close") 
